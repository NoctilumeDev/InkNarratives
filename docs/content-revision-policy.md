# 正文修订时间契约

“正文修订”回答的是一个窄而明确的问题：**读者正在阅读的作品正文，最后一次发生实质变化是什么时候？**

它不是文件修改时间，也不是 CSS、JavaScript 或部署流水线的更新时间。

## 计时范围

每件作品以其 HTML 中第一个 `<main>` 元素的可读文本为正文边界。校验器会：

1. 移除注释以及 `style`、`script`、`template`、`svg` 内容；
2. 去除其余 HTML 标签并解码字符实体；
3. 将连续空白折叠为单个空格；
4. 对所得 UTF-8 文本计算 SHA-256。

以下变化需要更新 `content-revised` 日期和 `docs/content-revisions.json` 中的指纹：

- 增删或改写标题、段落、引文、注释、图注等正文；
- 改变会影响读者理解的标签文案或章节顺序；
- 修正事实、人物、时间、出处或文字讹误。

以下变化不推进正文修订日期：

- CSS、动画、响应式布局和纯视觉资源调整；
- JavaScript 交互实现、性能优化和兼容性修复；
- URL、部署、CI、重定向与仓库文档变化；
- 不改变可读文本的语义标签或无障碍结构修复。

## 更新流程

正文发生实质变化时，应在同一个提交中完成三件事：

1. 更新作品 `<head>` 中的 `<meta name="content-revised">`；
2. 更新展厅展签中的 `<time datetime>`；
3. 更新 `docs/content-revisions.json` 的日期与文本指纹。

`node scripts/verify-repository.mjs` 会同时核对这三处。只改正文而忘记更新展签，或只改日期而没有对应正文指纹，都会使仓库门禁失败。

## 当前基线

五件作品的当前正文基线均来自 2026-08-08。此后的视口修复和统一展厅建设没有改变作品正文，因此不重写这个日期。
